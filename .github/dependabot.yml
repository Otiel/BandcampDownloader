version: 2
updates:
    -   package-ecosystem: "nuget"
        directory: "/src/"
        schedule:
            interval: "weekly"
        groups:
            Microsoft.Extensions:
                applies-to: version-updates
                patterns:
                    - "Microsoft.Extensions.*"
        groups:
            NUnit:
                applies-to: version-updates
                patterns:
                    - "nunit*"

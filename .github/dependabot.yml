version: 2
updates:
    -   package-ecosystem: "nuget"
        directory: "/src/"
        schedule:
            interval: "weekly"
        groups:
            bump-nunit:
                applies-to: version-updates
                patterns:
                    - "nunit*"
